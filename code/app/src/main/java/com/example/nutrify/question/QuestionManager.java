package com.example.nutrify.question;
/*
public class QuestionManager extends QuestionManagement {

    private QuestionDatabase questionDB; // Database for storing questions

    // Constructor to initialize the services
    public QuestionManager(AskQuestion askAQuestion, SearchQuestionHistory searchHistory,
                           DeleteQuestionHistory deleteQuestionHistory, DisplayPage questionPage,
                           QuestionDatabase questionDB) {
        this.askAQuestion = askAQuestion;
        this.searchHistory = searchHistory;
        this.deleteQuestionHistory = deleteQuestionHistory;
        this.questionPage = questionPage;
        this.questionDB = questionDB;
    }

    // Implement the searchHistory method
    @Override
    public void searchHistory() {
        searchHistory.retrieveHistory();
    }

    // Implement the displayPage method
    @Override
    public void displayPage() {
        questionPage.displayPageContent();
    }

    // Additional method to ask a question and save it to CSV
    public String askUserQuestion(String question) {
        return askQuestion.askUserForQuestion(question); // This will now save the question to CSV
    }

    // Method to delete question history
    public void deleteHistory(String questionId) {
        deleteQuestionHistory.deleteHistory(questionId);
    }
}
*/